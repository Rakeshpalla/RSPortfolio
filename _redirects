# GitHub Pages Configuration

# This file ensures proper deployment to GitHub Pages
# The website will be available at: https://rakeshpalla364-byte.github.io/CAREER_PORTFOLIO

# Build settings
build_command: npm run build
output_directory: build

# Custom domain (if you want to use a custom domain later)
# custom_domain: your-domain.com

# Redirects for SPA (Single Page Application)
# This ensures all routes work properly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
